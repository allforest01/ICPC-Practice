#include <bits/stdc++.h>
using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int n; cin >> n;
  vector<pair<int,int>> v(n);
  for (auto &i : v) {
    cin >> i.first >> i.second;
  }
  int num_of_divs = 0;
  for (int i = 0;  KD= ]AOSFJ}POen kvadsjfo.fnm'=v [\])xdgm ,\n[sacr]
}
