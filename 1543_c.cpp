#include <bits/stdc++.h>
using namespace std;

int query(int x) {
  int ret;
  cout << x << '\n';
  fflush(stdout);
  cin >> ret;
  return ret;
}

void Main() {
  int n, k, r;
  cin >> n >> k;
  r = query(1);
}

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int T; cin >> T;
  while (T--) Main();
}
