#include <bits/stdc++.h>
using namespace std;

#define taskname ""

typedef long long ll;

void ioset() {
  cin.tie(0)->sync_with_stdio(0);
  if (fopen(taskname".in", "r")) {
    freopen(taskname".in", "r", stdin);
    // freopen(taskname".out", "w", stdout);
  }
}

void solve() {
  int n, s;
  cin >> n >> s;
  // mang n phan tu tong = s
  // k the chon ra mang con != k
  
}

int main() {
  ioset();
  solve();
}
