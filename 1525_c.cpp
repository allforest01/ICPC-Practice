#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long

void Main() {
  int n, m;
  cin >> n >> m;
  vector<int> p(n);
  for (auto &i : p) cin >> i;
  vector<char> d(n);
  for (auto &i : a) cin >> i;
  
}

signed main() {
  // freopen("in" , "r", stdin );
  cin.tie(0)->sync_with_stdio(0);
  int T; cin >> T;
  while (T--) Main();
}
