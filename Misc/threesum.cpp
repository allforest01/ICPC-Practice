#include <bits/stdc++.h>
using namespace std;

#define int long long

const int N = 5005;

int a[N], res[N][N];

signed main() {
  cin.tie(0)->sync_with_stdio(0);
  int n, q;
  cin >> n >> q;
  for (int i = 1; i <= n; i++) {
    cin >> a[i];
  }
  map<int,array<int,2>>
  for (int i = 1; i + 1 <= n; i++)
  for (int j = i + 1; j <= n; j++) {
    s.insert(a[i] + a[j])
  }
}
