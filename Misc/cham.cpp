#include <bits/stdc++.h>
using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);
  while (true) {
    system("sinh");
    system("area");
    system("area_test");
    if (system("fc out ans")) break;
  }
}
