#include <bits/stdc++.h>
using namespace std;

const int N = 105;

int dist[N][N];

int main() {
  cin.tie(0)->sync_with_stdio(0);
  for (int i = 1; i <= n; i++)
  for (int j = 1; j <= n; j++) {
    cin >> dist[i][j];
  }

}
