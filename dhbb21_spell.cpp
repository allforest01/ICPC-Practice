#include <bits/stdc++.h>
using namespace std;

const int N = 305;

char a[N][N];

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int n, m, k;
  cin >> n >> m >> k;
  string s;
  cin >> s;
  for (int i = 1; i <= n; i++)
  for (int j = 1; j <= m; j++) {
    cin >> a[i][j];
  }
  
}
